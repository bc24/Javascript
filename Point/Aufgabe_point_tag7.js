/*
Aufgabe 1
Implementiere die Klasse Point und Line.
Orientiere dich am Klassendiagramm am Whiteboard

Aufgabe 2
Die Formeln für die Methoden Point.getDistance und Point.getAngle sind am Whiteboard zu finden

Aufgabe 3
Erstelle ein HTML Seite mit einem Canvas

Aufgabe 4
Implementiere die draw Methode aus der Klasse Line. Die draw Methode deklariert einen Parameter ctx. Der Parameter ctx stellt den Kontext zum Zeichen dar.

Aufgabe 5
Implementiere einen Mausevent zur Aufzeichnung von Mauseingaben. Jeder Klick setzt einen Punkt und erzeugt eine Point Instanz (max 2)

Point                                                       Line      
_____________________________                               _____________________________
x : Nubmer                                                  Pt1 : Point
y : Number                                                  Pt2 : Point
-----------------------------                               -----------------------------
create Point(angle,distance)                                draw(context)
getDistance(Point):Number                                   _____________________________
getAngle(Point:Number)
_____________________________

*/
<
html >
    <
    head > < /head> <
    body >
    <
    script >
    class Point {
        constructor(x, y) {
            this.x = y;
            this.y = y;
        }
        create Point(angle, distance)
        getDistance(Point): Number
        getAngle(Point: Number)
    }

class Line {
    constructor(x, y) {
            this.x = y;
            this.y = y;
            createPoint
        } <
        /script> <
        /body> <
        /html>